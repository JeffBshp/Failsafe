This folder contains resources that the game reads and writes at runtime.
When running the game, the working directory needs to contain this folder.

/code       source code that the player writes and runs in-game
/font       font and textures
/glsl       shader code
/world      terrain data
